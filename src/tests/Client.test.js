const { expect } = require('chai');
const Client = require('../Client.js');

suite('Client', () => {
    test('has a socket');

    suite('#write', () => {

    })
    
    suite('#prompt', () => {

    })
})